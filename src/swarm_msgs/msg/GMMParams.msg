# Gaussian Mixture Model Parameters

float64[] means          # Flattened list of means, e.g. [x1, y1, x2, y2, ...]
float64[] covariances    # Flattened covariances, e.g. for K components: [c11, c12, c21, c22, ... repeated K times]
float64[] weights        # Weights per component
